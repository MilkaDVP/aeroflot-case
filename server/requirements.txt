# Сервер. Внешних СУБД и брокеров нет: всё поднимается одной командой.
# Нижние границы — версии, на которых проект проверен. Код использует
# HTTP_422_UNPROCESSABLE_CONTENT, которой нет в ранних Starlette, а сама
# FastAPI допускает starlette>=0.46 — поэтому граница Starlette задана явно.
fastapi>=0.141
starlette>=1.6
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7

# Только для тестов: TestClient из FastAPI работает поверх httpx.
httpx>=0.27
